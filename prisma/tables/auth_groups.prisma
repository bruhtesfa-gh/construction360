/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model auth_groups {
  auth_group_id          String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  builder_id             String                   @db.Uuid
  name                   String                   @db.VarChar(150)
  created_at             DateTime?                @default(now()) @db.Timestamptz(6)
  updated_at             DateTime?                @default(now()) @db.Timestamptz(6)
  auth_group_permissions auth_group_permissions[]
  builders               builders                 @relation(fields: [builder_id], references: [builder_id], onDelete: Cascade, onUpdate: NoAction)
  user_groups            user_groups[]

  @@unique([builder_id, name])
  @@index([builder_id], map: "idx_auth_groups_builder_id")
  @@index([name], map: "idx_auth_groups_name")
}
